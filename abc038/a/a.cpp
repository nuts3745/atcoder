#include <bits/stdc++.h>
#define rep(i,n) for (int i = 0; i < (n); ++i)
using namespace std;
using ll = long long;
using P = pair<int,int>;

int main() {
    string s;
    cin >> s;
    if (s.at(s.size()-1) == 'T') cout << "YES" << endl;
    else cout << "NO" << endl;
    return 0;
}